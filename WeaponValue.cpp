#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<string> arr;
        for (int i = 0; i < n; i++)
        {
            string ele;
            cin >> ele;
            arr.push_back(ele);
        }
    }
    return 0;
}
