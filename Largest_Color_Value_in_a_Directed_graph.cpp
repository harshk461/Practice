#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    int largestPathValue(string colors, vector<vector<int>> &edges)
    {
        unordered_map<int, bool> vis;
        vector<int> freq(26, 0);
        unordered_map<int, bool> path_vis;
        
    }
};

int main()
{

    return 0;
}