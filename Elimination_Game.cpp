#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    int lastRemaining(int n)
    {
        deque<int> arr;
        for (int i = 1; i <= n; i++)
            arr.push_back(i);

        bool dirn = true; // left to right
        while (dq.size() == 1)
        {
        }
    }
};

int main()
{

    return 0;
}